"""ContextRank — cross-session context prioritization.

Scores files, symbols, and findings by combining reactive signals
(current session tool calls) with predictive signals (embedding
similarity to historical task inputs and step descriptions).
"""
