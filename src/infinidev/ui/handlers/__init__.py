"""UI handler modules — extracted from app.py for separation of concerns.

Each handler owns its domain state and logic, communicating with the
app (mediator) through a reference passed at construction time.
"""
