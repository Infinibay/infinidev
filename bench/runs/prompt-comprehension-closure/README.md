# Historical draft preflight closure

The two `pre-672.historical` artifacts preserve GPT-5.6 Sol's administrative closure of the former
432-case draft. They contain 1,296 `preflight_blocked` tuples and **zero provider calls**.

They are not an experimental campaign, are not inputs to current tests, and must not pin the active
authoring dataset. The active battery now contains 672 cases and remains mutable and unapproved
while semantic review and further authoring continue. A new campaign closure may be created only
after an approved dataset and immutable raw-only manifest exist.
