{"category": "defect_localization_at_scale", "expected_changed_paths": [], "family": "hidden-contract", "forbidden_changed_paths": ["src/pipeline.py", "src/hubs/*", "src/pkgs/*/__init__.py", "tests/*", "pyproject.toml", "verify_contract.py"], "id": "deep-localization", "repository_fixture": "deep_repo", "request": "pipeline(start) in src/pipeline.py returns start + 14349. Every leaf in the hierarchy applies one fixed positive offset and the packages and hubs only chain them, so the total must be start + 14398: exactly one leaf has the wrong offset. Find it, fix it, keep every public signature, and do not change src/pipeline.py, any src/pkgs/*/__init__.py, anything under tests/, or pyproject.toml. Run the tests to check your work.", "required_action_patterns": ["pytest"], "required_final_patterns": ["leaf", "test"], "review_status": "approved", "reviewer": "self-authored scale probe for the navigation-validity doubt", "rubric": [{"description": "pipeline(start) returns start + 14398 for every probed start.", "evidence_source": "verify_contract.py", "id": "total", "kind": "deterministic", "weight": 1.0}, {"description": "The fix corrects the leaf that is wrong rather than compensating in the pipeline or a hub.", "evidence_source": "the diff", "id": "located-the-leaf", "kind": "human_review", "weight": 0.8}, {"description": "The leaf is found by bisecting the hierarchy, not by reading twelve hundred files one at a time.", "evidence_source": "tool trace", "id": "localized-by-bisecting", "kind": "human_review", "weight": 0.5}], "split": "validation", "verify_command": "{python} verify_contract.py", "withheld_paths": ["verify_contract.py"]}
