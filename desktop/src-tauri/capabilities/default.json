{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities granted to the main window. File IO and engine access go through embedded Rust commands; the dialog plugin powers File → Open Folder.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
